version: '3.8'

services:
  db:
    image: mysql:latest
    container_name: mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: aula_integrada
    ports:
      - "3306:3306"
    volumes:
      - ./.docker/mysql:/var/lib/mysql
